# MangoDefend Desktop - Requirements
# Python 3.8+ (disarankan Python 3.11)
#
# Untuk menjalankan aplikasi dari source di VM:
#   py -m pip install -r requirements.txt
#   py main.py

# PySide6 - GUI Framework (Qt6 official binding, more stable on Windows)
PySide6>=6.6.0

# Image Processing + numeric array untuk representasi biner ke gambar
Pillow>=10.0.0
numpy>=1.24.0

# HTTP Client for Backend Communication
requests>=2.31.0
urllib3>=2.0.0

# Embedded Local Backend
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0

# Real-time Protection
# watchdog: memantau file baru/copy/rename/modified
# psutil: membaca dan mematikan proses
# wmi + pywin32: monitor proses baru di Windows dengan lebih cepat
watchdog>=3.0.0
psutil>=5.9.0
wmi>=1.5.1; platform_system=="Windows"
pywin32>=306; platform_system=="Windows"

# Notifications (Windows)
win10toast>=0.9; platform_system=="Windows"

# ONNX Runtime - For ML Model Inference (Faster than PyTorch)
onnxruntime>=1.16.0

# Archive scanning support
rarfile>=4.1
py7zr>=0.20.0

# Document/report helper scripts in this repo
python-docx>=1.1.0

# Development / model conversion tools
# Tidak wajib untuk menjalankan main.py, tetapi dipakai kalau konversi model .pth ke .onnx.
torch>=2.0.0
torchvision>=0.15.0
onnx>=1.15.0

# Plotting/Diagrams (for activity diagrams)
matplotlib>=3.7.0

# PyInstaller - For Building Executable
pyinstaller>=6.0.0

# Optional: for better performance on CUDA-enabled systems
# Install from: https://pytorch.org/get-started/locally/
# torch --index-url https://download.pytorch.org/whl/cu118
# torchvision --index-url https://download.pytorch.org/whl/cu118
# onnxruntime-gpu>=1.16.0  # For GPU acceleration with ONNX

